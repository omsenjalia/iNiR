#!/bin/bash
# Extra commands to run during ./setup install / update
#
# HOW TO USE:
#   Add active commands inside the function body, one per line.

custom_commands() {
    # Add your custom commands below:

    # Leave this no-op command to prevent syntax errors
    :
}
